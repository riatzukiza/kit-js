(include "./header")
(def timeout (t) (make-promise (set-timeout success) t))
;; (var f (async (=> (a)
;;                   (print "waiting a second")
;;                   (await (timeout (1000)))
;;                   (print "done"))))
(def-async f (a)
  (print "waiting a second")
  (await (timeout (1000)))
  (print "done")
  )
